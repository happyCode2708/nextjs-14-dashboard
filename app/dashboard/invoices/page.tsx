import { sleep } from '@/app/lib/utils';
const Dashboard_invoices_page = async () => {
  await sleep(1000);
  return <div> this is dashboard invoices page </div>;
};

export default Dashboard_invoices_page;
